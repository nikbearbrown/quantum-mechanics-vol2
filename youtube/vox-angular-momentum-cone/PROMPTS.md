# PROMPTS — vox-angular-momentum-cone
## Paste-ready generative prompts for open still slots

```
B09, angular momentum quantum cone diagram, a single white z-axis arrow pointing up on cream newsprint background, one teal angular momentum vector of fixed length sweeping on a cone around the z-axis, the vector's shadow projection onto the z-axis labeled "ell hbar" in ink serif, the total vector length labeled "hbar sqrt(ell(ell+1))" in ink serif, a dotted arc indicating the cone half-angle at the apex, flat editorial print style, no gradients, no glow, three objects only: axis, cone vector, projection line, Vox newsprint aesthetic
```
